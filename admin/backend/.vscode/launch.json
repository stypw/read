{
    "configurations": [
        {
            "name": "main",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}",
            "cwd": "${workspaceRoot}",
            "output": "main"
        }
    ]
}